{
    "name": "sable-dtl",
    "version": "0.1.0",
    "private": true,
    "type": "commonjs",
    "scripts": {
        "test": "node --test tests/node/*.test.js",
        "test:rust": "cargo test --locked",
        "test:all": "cargo test --locked && node --test tests/node/*.test.js",
        "ci": "bash scripts/ci.sh"
    },
    "engines": {
        "node": ">=20"
    }
}
