{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Run final scenario",
            "cargo": {
                "args": ["build", "--bin=sable_dtl"],
                "filter": {
                    "name": "sable_dtl",
                    "kind": "bin"
                }
            },
            "args": ["final"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
